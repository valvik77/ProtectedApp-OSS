const language=document.documentElement.lang;const button=document.querySelector('#lang');function render(next){document.documentElement.lang=next;document.querySelectorAll('[data-es]').forEach(e=>e.innerHTML=e.dataset[next]);button.textContent=next==='es'?'EN':'ES';localStorage.setItem('protectedapp-language',next)}render(localStorage.getItem('protectedapp-language')||language);button.addEventListener('click',()=>render(document.documentElement.lang==='es'?'en':'es'));
